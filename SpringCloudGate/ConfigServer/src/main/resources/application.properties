spring.application.name=ConfigServer

server.port=9000

# Config Server

# Connect Git Repo
spring.cloud.config.server.git.uri= ${GIT_REPO}
spring.cloud.config.server.git.ignore-local-ssh-settings=true
# Connect Private Key
spring.cloud.config.server.git.private-key= ${GIT_PRIVATE}